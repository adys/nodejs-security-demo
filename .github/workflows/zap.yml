name: zap

on:
  pull_request:
  push:
    branches: [master]

jobs:
  zap:
    runs-on: ubuntu-latest
    name: njssnodejsscancan check
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.4.0
        with:
          target: 'https://6n5m8z8ni9.execute-api.eu-central-1.amazonaws.com/dev'